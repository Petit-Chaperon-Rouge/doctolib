spring:
  datasource:
    url: jdbc:mysql://localhost:3355/cabinet_medical
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: WARN
    fr.semifir: INFO
  file:
    name: logs/logs.txt
    path: ./
