spring:
  datasource:
#    url: jdbc:mysql://localhost:3355/doctolib?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    url: jdbc:mysql://spring_db:3307/doctolib?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: toto
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: WARN
    fr.semifir: INFO
  file:
    name: logs/logs.txt
    path: ./
